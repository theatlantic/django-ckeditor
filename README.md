This directory should be in `ckeditor/static/ckeditor/ckeditor/_dev`, relative to the django-ckeditor package root.

To execute, run:

```
cd ckeditor/static/ckeditor/ckeditor/_dev/releaser
./release.sh
```
